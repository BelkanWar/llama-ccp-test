[project]
name = "llama-cpp-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "BelkanWar", email = "challenger1501@gmail.com" }
]
requires-python = ">=3.12.8"
dependencies = [
    "llama-cpp-python>=0.3.35",
    "openai>=3.13.0",
]

[project.scripts]
llama-cpp-test = "llama_cpp_test:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
